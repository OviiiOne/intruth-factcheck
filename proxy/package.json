{
  "name": "newspal-proxy",
  "version": "1.0.0",
  "description": "Proxy for NewsPal extension — forwards requests to the LLM providers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
